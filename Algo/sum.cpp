#include <iostream>
// Algo is improved because the second algo checks to see if the list is empty
int main(){

    def firstTria_sum(S):
    total = 0;
    for x in S:
        total += x
    return 0;

    def secondTrial_sum(S):
        if len(S) == 0
            return 0;
    else:
        total = 0
        for x in S:
            total += x
        return total
}