#include <iostream>

int main(){
    //brute force
    function sum_of_numbers(n):
        total = 0
        for i in range(1, n + 1):
            total += i
        return tota;

    // Mathmatical formula 
    function sum_of_nubmers(n):
        return n * (n + 1) / 2
}